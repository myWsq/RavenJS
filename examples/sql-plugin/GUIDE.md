# REQUIRED READING

- [README.md](./README.md) — understand what this example is for
- [index.ts](./index.ts) — see the minimal plugin implementation

# WHEN TO READ THIS

- Read this example when you need a database plugin pattern built on `definePlugin` + `defineAppState`.
- Do not treat this directory as an installable package or official extension point registry entry.
