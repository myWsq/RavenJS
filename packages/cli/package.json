{
  "name": "@ravenjs/cli",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "prebuild": "bun run scripts/generate-registry.ts",
    "build": "bun build ./index.ts --compile --minify --outfile raven"
  },
  "dependencies": {
    "cac": "^6.7.14",
    "ora": "^8.1.1",
    "picocolors": "^1.1.1",
    "yaml": "^2.6.0"
  }
}
