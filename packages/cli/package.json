{
  "name": "@ravenjs/cli",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "prebuild": "bun run scripts/generate-registry.ts",
    "build": "bun build ./index.ts --compile --minify --outfile raven"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "cac": "^6.7.14",
    "picocolors": "^1.1.1",
    "semver": "^7.7.4",
    "yaml": "^2.6.0"
  }
}
